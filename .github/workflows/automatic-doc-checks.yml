name: Main Documentation Checks

on:
  - push
  - pull_request
  - workflow_dispatch

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  documentation-checks:
    uses: fnordahl/documentation-workflows/.github/workflows/documentation-checks.yaml@upload-artifacts
    with:
      working-directory: 'docs'
